- Delete an entry from an ldap directory
   ldapdelete -ldapUrl ldap://directory.ldaptive.org \
              -dn cn=manager,ou=people,dc=ldaptive,dc=org

- Display all the command line options available 

   ldapdelete -help
